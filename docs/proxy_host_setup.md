# 設定 Proxy Host（第一次設定 NPM）

1. 打開 http://localhost:81 → 登入 NPM（預設帳密）
2. 新增 Proxy Host：
Domain: n8n.yourdomain.com

Forward IP: n8n

Forward port: 5678

開啟 Websockets 支援

勾選 SSL → 使用 Let's Encrypt → 輸入 Email → 勾選 Force SSL